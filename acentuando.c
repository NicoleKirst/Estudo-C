#include <stdio.h>
#include <stdlib.h>
int main(void) {
    int i = 10;

    // coloca %c no local que é para acentuar os elementos
    printf("O valor da vari%cvel %c: %d\n",160,130, i);

    system("pause");
    return 0;
}